---
name: Feature request
about: Propose a new capability or improvement
title: ''
labels: enhancement
assignees: ''
---

## What and why

<!-- The capability, and the concrete use case behind it. -->

## What it would look like in a note

<!-- If it touches authoring: sketch the frontmatter/markdown a user writes. -->

```markdown

```

## Notes

<!-- Optional. If you know it needs Inkdrop host API the plugin doesn't
     already use, say so — some things aren't reachable (see the "dropped"
     items in the roadmap note, e.g. a custom Preferences UI). Out of scope:
     PDF/HTML export (Inkdrop's built-in note export covers it). -->
